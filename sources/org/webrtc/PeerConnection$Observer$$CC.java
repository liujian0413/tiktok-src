package org.webrtc;

import org.webrtc.PeerConnection.Observer;
import org.webrtc.PeerConnection.PeerConnectionState;

public abstract /* synthetic */ class PeerConnection$Observer$$CC {
    public static void onConnectionChange(Observer observer, PeerConnectionState peerConnectionState) {
    }

    public static void onTrack(Observer observer, RtpTransceiver rtpTransceiver) {
    }
}
